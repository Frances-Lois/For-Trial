'use strict';
const path 	= require('path');

const config =  {
    DEVELOPMENT: {
        host: 'localhost',
        user: 'root',
        password: 'admin',
        database: 'MalICSi'
    },
}

module.exports = config;